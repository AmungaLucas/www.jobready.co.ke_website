export const metadata = {
  title: "Saved Jobs",
  description: "View and manage jobs you've bookmarked for later review on JobReady Kenya.",
};

export default function SavedJobsLayout({ children }) {
  return <>{children}</>;
}
