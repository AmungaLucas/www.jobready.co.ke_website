export const metadata = {
  title: "My Applications",
  description: "Track and manage your job applications on JobReady Kenya.",
};

export default function ApplicationsLayout({ children }) {
  return <>{children}</>;
}
