export const metadata = {
  title: "My Profile",
  description: "Manage your professional profile, CV details, skills, and education on JobReady Kenya.",
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}
